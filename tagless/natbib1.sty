\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{natbib1}[2007/07/14 Enhanced natbib by ccshan]
\RequirePackageWithOptions{natbib}

\expandafter\def
\expandafter\NAT@@open
\expandafter{%
\expandafter\the
\expandafter\NAT@@openprefix
\NAT@@open}

\newtoks\NAT@@openprefix
\def\NAT@mbox@prefix{\the\NAT@@openprefix
    \global\let\NAT@mbox\NAT@mbox@save
    \NAT@mbox}
\newcommand{\citearound}[1]{%
    \ifx\NAT@mbox\NAT@mbox@prefix\else
        \global\let\NAT@mbox@save\NAT@mbox
        \global\let\NAT@mbox\NAT@mbox@prefix
    \fi
    \global\NAT@@openprefix{\unskip#1\ \global\NAT@@openprefix{}}}
\newcommand{\citegenitive}{\citearound{'s}}
\newcommand{\citets}{\citegenitive\citet}
\newcommand{\Citets}{\citegenitive\Citet}
\newcommand{\citealts}{\citegenitive\citealt}
\newcommand{\Citealts}{\citegenitive\Citealt}
